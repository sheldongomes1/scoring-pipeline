output/*.csv
output/*.json
!output/.gitkeep
output_gcs_archive/
data/
__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Personal Claude Code session state (not source-of-truth)
.claude/settings.json
.claude/*.lock

# Pipeline runtime logs (regenerated per run)
logs/

# Analysis study outputs — regenerated by analysis/*.py scripts.
# Dated snapshots (results_v3_*, results_phaseB_*) are baselines preserved
# locally for diff-comparison; commit them explicitly with `git add -f` if
# you want them tracked in the repo.
analysis/results/
analysis/results_*/
